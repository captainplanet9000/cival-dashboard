# Development environment configuration
# Copy these settings to your .env.local file

# Enable mock data for development (avoids Supabase connection errors)
NEXT_PUBLIC_USE_MOCK_DATA=true

# Supabase configuration (used when not in mock mode)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Other environment variables from .env.example
# ...
